<%= form_for(@wholeseller_payment) do |f| %>
  <% if @wholeseller_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wholeseller_payment.errors.count, "error") %> prohibited this wholeseller_payment from being saved:</h2>

      <ul>
      <% @wholeseller_payment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
