<%= form_for(@bergain) do |f| %>
  <% if @bergain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bergain.errors.count, "error") %> prohibited this bergain from being saved:</h2>

      <ul>
      <% @bergain.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :survey_date %><br />
    <%= f.date_select :survey_date %>
  </div>
  <div class="field">
    <%= f.label :bergain_status %><br />
    <%= f.text_field :bergain_status %>
  </div>
  <div class="field">
    <%= f.label :bergain_price %><br />
    <%= f.number_field :bergain_price %>
  </div>
  <div class="field">
    <%= f.label :order_id %><br />
    <%= f.number_field :order_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
